A PDF-reader made in python and the PyQt6 library.

The application renders the PDF as a pixmap in a QWindow from the PyQt6.QtWidgets library and adds wingets from said library.

The ambition is to add the ability to annotate a PDF with markers.
